  /* 
    1.analiza el siguiente código y escribe qué se va a ejecutar:
    se ejecuta la opcion:...
  */
    if(true) {
      console.log("Opción 1");
    }
    else {
      console.log("Opción 2");
    }

  /* 
    2.analiza el siguiente código y escribe qué se va a ejecutar:
    se ejecuta la opcion:... opción 3
  */
    var numero1 = 20;
    var numero2 = 10;
    var resultado = numero1 + numero2;

    if(resutado < 15) {
      console.log("Opción 1")
    }
    else if(resultado === 25) {
      console.log("Opción 2")
    }
    else {
      console.log("Opción 3")
    }

  /* 
    3.analiza el siguiente código y escribe qué se va a ejecutar:
    se ejecuta la opcion:... opción 2
  */

    let alumno = "Alberto";

    if(alumno === "Juan") {
      console.log("Opción 1")
    }

    else if(alumno === "Alberto") {
      console.log("Opción 2")
    }

    else {
      console.log("Opción 3")
    }

  /* 
    4.Crea un programa que pregunte la edad de una persona, y responderle
    si tiene edad de irse a una misión o no. Para simplificar puedes elegir la edad
    del hombre o de la mujer.
    Pistas: Puede que las condiciones te sirvan de mucho, utiliza variables para guardar lo que consideres.
    Puedes usar el console.log() para dar el mensaje de resultado.
  */


    var edad = 18; 
    var sexo = "hombre"; 
    var estado = "casado"; 

    if(edad >= 18) {
      console.log("Tenés la edad para salir a la misión")
    }
    else if(edad < 18) {
      console.log("No podés salir.")
    }
    else(sexo === hombre); {
      console.log("Podés salir a la misión.")
    }
    

  /* 
    5.analiza el siguiente código y escribe qué se va a ejecutar:
    se ejecuta la opcion:... opción 3
  */

    var nombre = "Daniela";
    var edad = 52;
    var miembro = true;
    

    var mensaje = "";

    if(nombre.length > 5 || edad < 20) {
      var mensaje = "Acabas de entrar a opcion 1";
    }

    else if(nombre.length > 7 && edad === 52) {
      var mensaje = "Acabas de entrar a opcion 2";
    }

    else if(nombre.length === 7 && edad > 20) {
      var mensaje = "Acabas de entrar a opcion 3";
    }

  /* 
    6.Este código trae errores. Revisalo detenidamente y hazlo funcionar.
    Funciona?? Ahora.. analiza que devuelve el primer console (debes quitar los //)
    Ahora, analiza que devuelve el segundo console (para probarlo quita los //)
    Revisa los consoles de forma separada.
  */
    var miNombre = "Camilo";

    for(let i = 0 i < miNombre.lengt; i++) {
      // console.log(i);
      // console.log(miNombre[i]);
    }

  /* 
    7.Imprime con un console.log los numeros del 1 al 100
  */
    for(let i = 1;i <= 100;i++){
      console.log(i);
  }

  /* 
    8.Crea un archivo txt y explica con palabras sencillas qué es un bucle, para qué sirven las condiciones.
  Explica qué utilidad tienen los operadores logicos.
  */

