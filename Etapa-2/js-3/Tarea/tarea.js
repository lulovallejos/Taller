//NOTA: LAs funciones se utilizan para hacer una utilidad con el mismo código.
//Muchas veces necesitarás cambiar ciertas variables(parámetros) por lo que siempre debes
//Hacer que tus funciones sirvan con cualquier tipo de parámetros.

//1.Permite que la funcion retorne el resultado de la suma de ambos numeros.
function suma(a, b) {
  //tu codigo debajo
  
}

suma(5, 10);

//2.Permite que la funcion retorne el resultado de la resta de ambos numeros.
function resta() {
  //tu codigo debajo

}

resta(20, 12);

//3.Permite que la funcion retorne el resultado de la multiplicacion de ambos numeros.
function multiplicacion() {
  //tu codigo debajo

}

multiplicacion(8, 3);

//4.Permite que la funcion retorne el resultado de la division de ambos numeros.
function division() {
  //tu codigo debajo

}

division(20, 6);

//5.Te llegarán dos frases. Concatenalas, pero revisalas bien para que el retorno tenga sentido.
function frase() {
  //tu codigo debajo

}

frase("luego limpiaré los platos","Preparé mi comida");

//6.Retorna true si los dos valores son iguales, o retorna false de lo contrario
function comparacion() {
  //tu codigo debajo

} 

comparacion(8, 12);

//7.Suma la cantidad de caracteres de dos nombres que te llegarán por parámetro
function sumaCaracteres() {
  //tu codigo debajo

} 

sumaCaracteres("Ramon", "Antonella");


//8.De nuevo te daré dos nombres. Retorna el nombre con el length mas alto. 
// Si los dos nombres tienen la misma longitud retorna un string que diga: "Tiene el mismo length " + {agrega aqui el length}
//Recuerda que los parametros pueden 
//cambiar pero tu codigo debe servir igual de bien

function nombreLargo() {
  //tu codigo debajo

} 

nombreLargo("Joaquin", "Natanael");

//9.Saluda a una persona por su nombre
function sumaCaracteres(nombre) {
  //tu codigo debajo
  return "que tengas un buen día"
} 

sumaCaracteres("Andres");

//10.Te daré una lista de numeros. Crearás una variable donde vas a duplicar el array, y a cada elemento del array
// le vas a agregar este simbolo ° ejemplo: 1°, 2°, 3°, 4°, 5°, luego retorna esa copia editada del array
function editarArray(array) {
  //tu codigo debajo
  
} 

sumaCaracteres([1, 2, 3, 4, 5]);